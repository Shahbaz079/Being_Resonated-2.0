import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: { domains: ['plus.unsplash.com','img.clerk.com','images.unsplash.com','files.edgestore.dev'], // Add the hostname here
   },
};

export default nextConfig;
