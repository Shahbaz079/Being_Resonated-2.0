import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: { domains: ['plus.unsplash.com','images.unsplash.com','img.clerk.com','files.edgestore.dev'], // Add the hostname here
   },
};

export default nextConfig;
